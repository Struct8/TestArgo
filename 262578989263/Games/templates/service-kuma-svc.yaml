apiVersion: v1
kind: Service
metadata:
  name: kuma-svc
  namespace: gamestest
  labels:
    app.kubernetes.io/name: kuma-svc
    app.kubernetes.io/managed-by: struct8
    app.kubernetes.io/instance: Games
    cloudman.io/user: rmay-struct
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  type: ClusterIP
  ports:
    - name: http-80
      port: 80
      targetPort: 3001
      protocol: TCP
    - name: port-3001
      port: 3001
      targetPort: 3001
      protocol: TCP
  selector:
    app.kubernetes.io/name: Kuma
