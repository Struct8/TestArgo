apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: struct8-agent
  namespace: argocd1
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/name: struct8-agent
    app.kubernetes.io/managed-by: struct8
    app.kubernetes.io/instance: struct8-agent
    cloudman.io/creator: rmay-struct
  annotations:
    argocd.argoproj.io/sync-wave: "-10"
spec:
  project: proj2
  source:
    repoURL: "https://github.com/Struct8/TestArgo.git"
    targetRevision: HEAD
    path: 262578989263/struct8-agent/templates
  destination:
    server: "https://kubernetes.default.svc"
    namespace: struct8-agent
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
