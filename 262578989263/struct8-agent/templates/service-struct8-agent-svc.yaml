apiVersion: v1
kind: Service
metadata:
  name: struct8-agent-svc
  namespace: struct8-agent
  labels:
    app.kubernetes.io/name: struct8-agent-svc
    app.kubernetes.io/managed-by: struct8
    app.kubernetes.io/instance: struct8-agent
    cloudman.io/user: rmay-struct
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  type: ClusterIP
  ports:
    - name: http-80
      port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: struct8-agent
