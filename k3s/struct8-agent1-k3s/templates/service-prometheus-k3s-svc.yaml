apiVersion: v1
kind: Service
metadata:
  name: prometheus-k3s-svc
  namespace: struct8-agent-k3s
  labels:
    app.kubernetes.io/name: prometheus-k3s-svc
    app.kubernetes.io/managed-by: struct8
    app.kubernetes.io/instance: struct8-agent1-k3s
    cloudman.io/creator: rmay-struct
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  type: ClusterIP
  ports:
    - name: http-80
      port: 80
      targetPort: 9090
      protocol: TCP
  selector:
    prometheus: prometheus-k3s
